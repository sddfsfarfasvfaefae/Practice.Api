<Window x:Class="Practice.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Practice.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Background="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}">
            <StackPanel Width="300"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

                <TextBlock Text="Авторизация"
                   FontSize="30"
                   Margin="0 0 0 20"
                   HorizontalAlignment="Center" FontFamily="Times New Roman" FontStyle="Italic"/>

                <TextBlock Text="Логин" FontSize="16" FontFamily="Verdana"/>

                <TextBox x:Name="LoginBox"
                 Height="30"
                 Margin="0 0 0 10" TextChanged="LoginBox_TextChanged" Background="#4CBBBBBB"/>

                <TextBlock Text="Пароль" FontFamily="Verdana" FontSize="16"/>

                <PasswordBox x:Name="PasswordBox"
                     Height="30"
                     Margin="0 0 0 20" Background="#4CBBBBBB"/>

                <Button Content="Войти"
                Height="35"
                Click="LoginButton_Click" Background="#FF36D673" FontFamily="Verdana" FontSize="16"/>

            </StackPanel>
        </Grid>
    </Grid>
</Window>
